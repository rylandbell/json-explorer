var React = require('react');

//Displays the path string needed to reference the chosen path:
module.exports = ({ reduxState }) => (
  <label>
    <input type="checkbox"/> Use bracket notation for key names
  </label>
);
